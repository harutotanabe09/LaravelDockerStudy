version: "3.1"
services:
  web:
    image: nginx:latest
    ports:
      - 80:80
    volumes:
      - ./docker/web/default.conf:/etc/nginx/conf.d/default.conf
      - .:/var/www/html
    depends_on:
      - php-fpm
  php-fpm:
    image: php:7.4.1-fpm-alpine
    volumes:
      - .:/var/www/html